# Extract coefficients from fitted model_spec

Extract coefficients from fitted model_spec

## Usage

``` r
# S3 method for class 'fitted_model_spec'
coef(object, ...)
```

## Arguments

- object:

  A `fitted_model_spec` object

- ...:

  Additional arguments (ignored)

## Value

Named numeric vector of parameter estimates
