# Extract model specification from pair

Extract model specification from pair

## Usage

``` r
spec_a(pair)

spec_b(pair)
```

## Arguments

- pair:

  An `equivalence_pair` object

## Value

A `model_spec` object
