# Check if models have same parameter count

Check if models have same parameter count

## Usage

``` r
same_par_count(pair)
```

## Arguments

- pair:

  An `equivalence_pair` object

## Value

Logical
