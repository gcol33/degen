# Check if two models are equivalent

Check if two models are equivalent

## Usage

``` r
are_equivalent(x, model1, model2)
```

## Arguments

- x:

  An `equiv_classes` object

- model1:

  Name of first model

- model2:

  Name of second model

## Value

Logical
