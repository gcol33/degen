% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/symbolic.R
\name{symbolic_fisher}
\alias{symbolic_fisher}
\title{Symbolic Fisher information}
\usage{
symbolic_fisher(spec, y, par, method = c("auto", "analytical", "numerical"))
}
\arguments{
\item{spec}{A \code{model_spec} object}

\item{y}{Numeric vector of observed data (used for numerical fallback and n)}

\item{par}{Named numeric vector of parameter values}

\item{method}{Character; "auto" tries analytical first, "analytical" requires
closed-form, "numerical" forces numerical computation}
}
\value{
A \code{fisher_info} object containing the Fisher information matrix
}
\description{
Compute Fisher information using closed-form analytical formulas for
standard distributions. Falls back to numerical computation if no
analytical formula is available.
}
\details{
Analytical formulas are available for:
\itemize{
\item Normal (mu, sigma): I = diag(n/sigma^2, 2n/sigma^2)
\item Exponential (rate): I = n/rate^2
\item Poisson (lambda): I = n/lambda
\item Gamma (shape, rate): Closed-form involving digamma/trigamma
\item Binomial (prob): I = n/(prob * (1-prob))
\item Beta (shape1, shape2): Closed-form involving trigamma
\item Log-normal (mu, sigma): Similar to normal on log scale
}

The function attempts to detect the distribution from the model name.
}
\examples{
spec <- model_spec_normal()
y <- rnorm(100, mean = 5, sd = 2)

# Analytical computation
info <- symbolic_fisher(spec, y, par = c(mu = 5, sigma = 2))
print(info)
}
