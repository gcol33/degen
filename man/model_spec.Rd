% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_spec.R
\name{model_spec}
\alias{model_spec}
\title{Create a model specification}
\usage{
model_spec(
  loglik_fn,
  par_names,
  par_bounds = NULL,
  name = NULL,
  validate = TRUE
)
}
\arguments{
\item{loglik_fn}{A function with signature \verb{function(y, ...)} where \code{y} is
the data and \code{...} are named parameters. Must return a scalar log-likelihood.}

\item{par_names}{Character vector of parameter names. Must match the argument
names in \code{loglik_fn} (excluding \code{y}).}

\item{par_bounds}{Optional named list of parameter bounds. Each element should
be a length-2 numeric vector \code{c(lower, upper)}. Parameters not specified
default to \code{c(-Inf, Inf)}.}

\item{name}{Optional character string naming the model. If \code{NULL}, a name is
generated from the parameter names.}

\item{validate}{Logical; if \code{TRUE} (default), validate inputs on construction.
Set to \code{FALSE} for performance in tight loops.}
}
\value{
An S3 object of class \code{model_spec} with components:
\describe{
\item{loglik_fn}{The log-likelihood function}
\item{par_names}{Character vector of parameter names}
\item{par_bounds}{Named list of bounds for each parameter}
\item{n_par}{Number of parameters}
\item{name}{Model name}
}
}
\description{
Define a parametric model through its log-likelihood function. This is the
foundational object for equivalence and identifiability analysis.
}
\examples{
# Exponential model
exp_spec <- model_spec(
  loglik_fn = function(y, lambda) sum(dexp(y, rate = lambda, log = TRUE)),
  par_names = "lambda",
  par_bounds = list(lambda = c(1e-6, Inf)),
  name = "Exponential"
)

# Normal model with two parameters
norm_spec <- model_spec(
  loglik_fn = function(y, mu, sigma) {
    sum(dnorm(y, mean = mu, sd = sigma, log = TRUE))
  },
  par_names = c("mu", "sigma"),
  par_bounds = list(sigma = c(1e-6, Inf))
)
}
