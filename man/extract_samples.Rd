% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesian.R
\name{extract_samples}
\alias{extract_samples}
\title{Extract posterior samples from fitted objects}
\usage{
extract_samples(fit, pars = NULL)
}
\arguments{
\item{fit}{A fitted model object from rstan, brms, rstanarm, or similar}

\item{pars}{Optional character vector of parameter names to extract}
}
\value{
A matrix of posterior samples (rows = samples, columns = parameters)
}
\description{
Helper function to extract posterior samples from common Bayesian
modeling packages.
}
\details{
This function provides a unified interface for extracting posterior samples
from various Bayesian modeling packages. Currently supports:
\itemize{
\item stanfit objects (rstan)
\item brmsfit objects (brms)
\item stanreg objects (rstanarm)
\item matrix/data.frame (returned as-is)
}
}
\examples{
# With a matrix of samples (manual specification)
samples <- matrix(rnorm(200), ncol = 2)
colnames(samples) <- c("mu", "sigma")
extracted <- extract_samples(samples)
}
