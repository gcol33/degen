% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{compare_all_pairs}
\alias{compare_all_pairs}
\title{Compare all pairs of models}
\usage{
compare_all_pairs(
  models,
  y,
  n_points = 50,
  tol = 1e-06,
  verbose = FALSE,
  cl = NULL
)
}
\arguments{
\item{models}{Named list of \code{model_spec} objects}

\item{y}{Numeric vector of observed data}

\item{n_points}{Number of parameter points to sample per comparison}

\item{tol}{Tolerance for likelihood equality}

\item{verbose}{Logical; print progress information}

\item{cl}{Optional parallel cluster from \code{setup_cluster()}}
}
\value{
A list with:
\describe{
\item{comparisons}{List of \code{surface_comparison} results}
\item{matrix}{Logical matrix of pairwise equivalence}
\item{n_equivalent}{Number of equivalent pairs}
\item{n_total}{Total number of comparisons}
}
}
\description{
Perform pairwise comparisons between all models in a list.
}
\examples{
exp_spec <- model_spec_exponential()
gamma1_spec <- model_spec_gamma(par = "rate", known_shape = 1)
gamma2_spec <- model_spec_gamma(par = "rate", known_shape = 2)
models <- list(exp = exp_spec, gamma1 = gamma1_spec, gamma2 = gamma2_spec)
y <- rexp(50, rate = 2)
result <- compare_all_pairs(models, y, n_points = 10)
result$matrix
}
