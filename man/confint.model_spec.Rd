% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identifiability.R
\name{confint.model_spec}
\alias{confint.model_spec}
\title{S3 method for confint}
\usage{
\method{confint}{model_spec}(object, parm, level = 0.95, y, par, ...)
}
\arguments{
\item{object}{A \code{model_spec} object}

\item{parm}{Parameter names (character vector). If missing, all parameters.}

\item{level}{Confidence level (default 0.95)}

\item{y}{Numeric vector of observed data}

\item{par}{Named numeric vector of parameter values (typically MLE)}

\item{...}{Additional arguments (ignored)}
}
\value{
A matrix with columns for lower and upper bounds
}
\description{
Compute confidence intervals for model parameters using profile likelihood.
}
\examples{
spec <- model_spec(
  loglik_fn = function(y, mu, sigma) sum(dnorm(y, mu, sigma, log = TRUE)),
  par_names = c("mu", "sigma"),
  par_bounds = list(sigma = c(1e-6, Inf))
)
set.seed(123)
y <- rnorm(100, mean = 5, sd = 2)
confint(spec, y = y, par = c(mu = 5, sigma = 2))
}
