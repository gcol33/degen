% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_profile_likelihood}
\alias{plot_profile_likelihood}
\title{Plot profile likelihood for a parameter}
\usage{
plot_profile_likelihood(profile, ci_level = 0.95, mle = NULL, ...)
}
\arguments{
\item{profile}{A data frame from \code{profile_likelihood()} or parameter name}

\item{ci_level}{Confidence level for interval (default 0.95)}

\item{mle}{Optional MLE value to mark on plot}

\item{...}{Additional arguments passed to \code{plot()}}
}
\value{
Invisible profile data
}
\description{
Plot the profile likelihood curve for a single parameter with confidence
interval bounds.
}
\examples{
spec <- model_spec(
  loglik_fn = function(y, mu, sigma) sum(dnorm(y, mu, sigma, log = TRUE)),
  par_names = c("mu", "sigma"),
  par_bounds = list(sigma = c(1e-6, Inf))
)
y <- rnorm(100, mean = 5, sd = 2)
prof <- profile_likelihood(spec, y, par = c(mu = 5, sigma = 2), which_par = "mu")
plot_profile_likelihood(prof, mle = 5)
}
