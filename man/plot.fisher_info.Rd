% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.fisher_info}
\alias{plot.fisher_info}
\title{Plot Fisher information diagnostics}
\usage{
\method{plot}{fisher_info}(x, type = c("eigenvalues", "loadings", "both"), ...)
}
\arguments{
\item{x}{A \code{fisher_info} object}

\item{type}{Type of plot: "eigenvalues" (default), "loadings", or "both"}

\item{...}{Additional arguments passed to plotting functions}
}
\value{
Invisible \code{x}
}
\description{
Visualize the eigenvalue spectrum and parameter loadings of a Fisher
information matrix.
}
\examples{
spec <- model_spec(
  loglik_fn = function(y, mu, sigma) sum(dnorm(y, mu, sigma, log = TRUE)),
  par_names = c("mu", "sigma"),
  par_bounds = list(sigma = c(1e-6, Inf))
)
y <- rnorm(100, mean = 5, sd = 2)
info <- fisher_information(spec, y, par = c(mu = 5, sigma = 2))
plot(info)
plot(info, type = "loadings")
}
