% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate.model_spec}
\alias{simulate.model_spec}
\title{Simulate data from a model specification}
\usage{
\method{simulate}{model_spec}(object, nsim = 1, seed = NULL, n, par, ...)
}
\arguments{
\item{object}{A \code{model_spec} object with simulation capability}

\item{nsim}{Number of simulations (datasets) to generate}

\item{seed}{Random seed for reproducibility}

\item{n}{Number of observations per simulation}

\item{par}{Named numeric vector of parameter values}

\item{...}{Additional arguments (ignored)}
}
\value{
If \code{nsim = 1}, a numeric vector. If \code{nsim > 1}, a list of vectors.
}
\description{
Generate random data from a model_spec that has a simulation function
attached via \code{add_simulator()}.
}
\examples{
spec <- model_spec_normal()
spec <- add_simulator(spec, function(n, mu, sigma) rnorm(n, mu, sigma))
y <- simulate(spec, n = 100, par = c(mu = 5, sigma = 2))
hist(y)
}
